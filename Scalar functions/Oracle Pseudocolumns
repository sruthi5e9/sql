CURRVAL and NEXTVAL
LEVEL
ROWID
ROWNUM

SELECT STUDENTSEQ.currval FROM DUAL;
INSERT INTO STUDENT VALUES (STUDENTSEQ.nextval, ‘DEVI’, ‘PYTHON’, 1901);


SELECT * FROM EMP WHERE ROWNUM <= 3; 

SELECT ROWID, ename FROM emp WHERE deptno = 20;
